Updated the unreal feature to now pass arguments without using JSON strings
Renamed the git additional function `checkout` to `projectCheckout` to avoid naming collision with the function from the jenkins git plugin